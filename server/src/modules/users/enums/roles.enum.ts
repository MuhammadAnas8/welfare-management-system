export enum GlobalRole {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  EDITOR = 'editor',
  VIEWER = 'viewer',
}

export enum BranchRole {
  ADMIN = 'branch_admin',
  EDITOR = 'branch_editor',
  VIEWER = 'branch_viewer',
}
